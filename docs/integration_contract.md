# Integration Contract

The demo writes `outputs/final_result.json` with:

- task id and type
- agent ENS and wallet metadata
- mock 0G output and evaluation URIs
- output and evaluation hashes
- final scores
- winner metadata
- total reward amount
